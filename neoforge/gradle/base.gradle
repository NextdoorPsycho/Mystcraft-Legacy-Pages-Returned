jar {
    archiveFileName = "mystcraft_legacy-${version}-${minecraft_version}-neoforge-${neoforge_version}.jar"
}

sourcesJar {
    archiveFileName = "Mystcrafy Legacy - ${version} - NeoForge-sources.jar"
}
